name: engine-core
version: 0.1.0

dependencies:
  action-controller:
    github: spider-gazelle/action-controller

  engine-driver:
    github: aca-labs/crystal-engine-driver

  engine-rest-api: # This should be split into a seperate models library.
    github: aca-labs/crystal-engine-rest-api

  etcd:
    github: aca-labs/crystal-etcd

  hound-dog:
    github: aca-labs/hound-dog

development_dependencies:
  ameba:
    github: veelenga/ameba

# compile target
targets:
  engine-core:
    main: src/engine-core.cr
